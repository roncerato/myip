import { StyleSheet } from "react-native";

export const NetworkDataStyles = StyleSheet.create({
    container: {
        marginTop: 16,
        flexDirection: "column",
        gap: 16
    },
    data:{
        flexDirection: "row", 
        gap: 16,
    }
})